https://github.com/qinjx/30min_guides/blob/master/shell.md

学习到的点：
  如果有一大段需要注释，可以用花括号起来，定义成一个函数，没有地方调用这个函数，这块代码就不会执行，达到了和注释一样的效果。
  单引号字符串：（双引号不影响，可以有变量也可以用转义符）
    单引号里的任何字符都会原样输出，单引号字符串中的变量是无效的，eg: a=1; echo 'a$a' 输出： a$a
    单引号字串中不能出现单引号（对单引号使用转义符后也不行）
  
